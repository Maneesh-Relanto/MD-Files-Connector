# some-pkg

A third-party package installed in node_modules.
This file must NEVER appear in MD Files Connector output — it lives inside
an excluded directory (node_modules) and should be invisible to the scan.
