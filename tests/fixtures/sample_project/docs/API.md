# API Reference

This file is **intentionally not linked** from the project README.
It is used to test that MD Files Connector correctly identifies isolated files.

## Endpoints

### GET /health

Returns `200 OK` when the service is running.

### POST /scan

Triggers a full scan of the target directory.
