# Configuration

All available configuration options for Test Project 1.

## Environment variables

| Variable | Default | Description |
|---|---|---|
| `APP_PORT` | `8080` | Port the server listens on |
| `APP_DEBUG` | `false` | Enable debug logging |
| `APP_DB_URL` | — | Database connection string |

## Config file

Place a `config.yml` at the project root to override defaults.
